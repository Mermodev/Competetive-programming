---
tags:
  - math
  - game_theory
---

# 2196A.cpp

```cpp
#include<bits/stdc++.h>
using namespace std;

int main(){
  cin.tie(0) -> sync_with_stdio(0);
  int Q;
  cin >> Q;
  while(Q--){
    long long x, y;
    cin >> x >> y;
    if(2 * y > 3 * x || x >= y)
      cout << "Alice\n";
    else 
      cout << "Bob\n";
  }
}
```

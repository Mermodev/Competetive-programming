#include<bits/stdc++.h>
using namespace std;

#define LL long long
#define ssize(x) int(x.size())

int main(int argc, char*argv[]){
  int SEED = stoi(argv[1]);
  mt19937 mt(SEED);
  // BODY HERE
  return 0;
}
